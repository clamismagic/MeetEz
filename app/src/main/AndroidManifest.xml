<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="name.calvin.meetez">

    <uses-sdk android:minSdkVersion="16" />

    <permission
        android:name="name.calvin.meetez.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="name.calvin.meetez.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Participants">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Dashboard"
            android:label="@string/app_name" />
        <activity
            android:name=".Description"
            android:label="@string/app_name" />
        <activity
            android:name=".splashScreen"
            android:label="@string/app_name" />
        <activity
            android:name=".AddParticipant"
            android:label="@string/app_name" />
        <activity
            android:name=".adHocMeeting"
            android:label="@string/app_name" />
        <activity
            android:name=".CreateEvent"
            android:label="@string/app_name"/>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDt6i9mPblgCX2cNlDPqxS40kkfqCDFz3E" />

        <activity android:name=".midpoint"></activity>
    </application>

</manifest>